#include <iostream>
#include "Iterator.h"
#include "MenuItem.h"
#include "Collection.h"


#define MAX_SIZE 10
using namespace std;
/*
void printMenu(Iterator *iter) {

	while (iter->hasNext()) {
		MenuItem m = iter->next();
		m.print();
	}
}
*/
int main() {

	/*
	MyCollection *breakfastMenu=new ArrayCollection();	
		
	breakfastMenu->append(MenuItem("Idli", "Soft Rice Idli with sambhar", 20.50, true));
	breakfastMenu->append(MenuItem("Puri", "Made with aata with sabji", 25.26, true));
	breakfastMenu->append(MenuItem("BreadAmlet", "Bread with two eggs", 10.23, false));
	breakfastMenu->append(MenuItem("Paratha", "Aalo paratha with chatni", 12.23, true));
	breakfastMenu->append(MenuItem("Chicken Paratha", "Chicken paratha with curd", 35.23, false));

	Iterator *breakfastIter = breakfastMenu->createIterator();

	printMenu(breakfastIter);


	delete breakfastMenu;
	delete breakfastIter;
	*/
	
	return 0;
}